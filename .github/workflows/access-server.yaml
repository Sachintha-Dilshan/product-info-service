name: CI [Manual] Access Server

on:
  workflow_dispatch:

jobs:
  access_server:
    name: Access server
    runs-on: ubuntu-latest
    steps:
      - name: Create a file
        uses: appleboy/ssh-action@master
        with:
          host: 13.53.129.105
          username: ubuntu
          key: ${{ secrets.PRIVATE_KEY }}
          script: echo hello world > hello.txt